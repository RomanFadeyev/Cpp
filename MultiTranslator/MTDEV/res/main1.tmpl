//------------------------------------------------------------------------------
int main()
{
	// first parameter - own name
	if (ParamCount<3)
	{
		print("Enter 2 parameters: input and output filename.\n");
		return 1;
	}

	print("Input filename: ",ParamStr[1],"\n");
	print("Output filename:   ",ParamStr[2],"\n");
	print("\n");

	// Set parameters for parsing text. This parameters are used by lexical analyzer
	IgnoreCase=$IGNORE_CASE$;
	//Uncomment necessary parameters
	//SolitaryWords=off; //on by default
	//IgnoreWhiteSpaces=off; //on by default
	//Delimiters="type delimiters here"; //all punctuation characters by default

	$IGNORE_BLOCK$
	$REPLACE_BLOCK$

	// Set input and output files
	SourceFileName  = ParamStr[1];
	DestFileName	= ParamStr[2];

	// Go parsing
 	write(	"\n/* --- File was generated by multitranslator, ---*/\n");
	
	if (!Parse("$ROOT_RULE$"))
	{
		print("Parsing failed\n");
		return 1;
	}

	SaveAllFiles();	
	print("Parsing succeeded\n");
}
